# Backlog

Keep only ordered, near-term, actionable work. Each item should say what “done” means. Remove completed items; Git history records completed implementation.

1. **M2: tool-list expansion** — exhaustive review of the legacy MCP's derived match facets, cross-referenced against HenrikDev's current API shapes (not legacy's possibly-stale assumptions); build whatever facets meet the existing bar (factual, context-efficient, no coaching/editorial output) as new MCP tools. Done when the review is complete and every surviving facet is a shipped, tested tool.
